# JARA International Website — Environment variables template
# Copy to .env.local for local dev. Cloudflare Pages env vars set via dashboard.
# NEVER commit .env.local or .env.production with real values.

# --- n8n webhook URLs (Phase 4 forms) ---
# Per Phase 1 constraint C1: PRODUCTION webhook (not /webhook-test/)
N8N_SUBMITTAL_WEBHOOK_URL=https://your-n8n-host/webhook/PRODUCTION_ID_HERE
N8N_DOCUMENT_REQUEST_WEBHOOK_URL=https://your-n8n-host/webhook/PRODUCTION_ID_HERE
# Optional shared secret header for webhook auth (recommend setting)
N8N_WEBHOOK_SECRET=

# --- Google Search Console verification (DNS TXT also works) ---
# Replace placeholder in app/layout.tsx after adding property in GSC
GSC_VERIFICATION_CODE=

# --- Bing Webmaster Tools verification ---
BING_VERIFICATION_CODE=

# --- Cloudflare Web Analytics (no token needed for embed beacon) ---
NEXT_PUBLIC_CF_BEACON_TOKEN=

# --- Site config ---
NEXT_PUBLIC_SITE_URL=https://jarainternational.com
